+++
title = "the foot's call carries only its route-local addresses, which behind a carrier or tethered NAT are private: read the DHT-observed address (BEP 42 ip) and put it in the call, as yog bl-efae does for presence"
created = 1790394967
updated = 1790394967
priority = 2
root_commit = "32be9f81c8ae1d50610ed025db0de83568b6736b"
+++
yog REMOTE §13.2 rules an endpoint list must carry the OBSERVED endpoint; §13.8 measured that on a cellular path the observed one is the only one there is. thrall bl-921e ported yog's walk but skipped yog bl-efae's observed-address vote because a foot publishes no presence — but the foot's sealed CALL (src/rendezvous/call.rs) is an endpoint list too, and it lists only route-local addresses, so a foot behind a carrier or tethered NAT hands the engine an address it cannot punch to. Port yog bl-efae (yog 022253e0: krpc reply ip, both lengths; observed() per-family majority vote over the last walk, tie gives nothing, a dark walk clears it) and append each observed address not already listed to the call at the foot's punch port — address only, never the observed port (yog REMOTE §13.2; the port-rewrite carrier case stays open, §13.8). Tests: parser both lengths + garbage; 3-vs-1 majority, tie none; call with observed present/absent round-tripped through the unseal; no duplicate of a local address. State it in DESIGN. Companion: yog-android bl-544b, same defect on the phone.