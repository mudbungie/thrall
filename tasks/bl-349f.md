+++
title = "found thrall: repository, gate, skeleton, design"
created = 1787977346
updated = 1787977372
claimant = "OrderFounder"
priority = 3
root_commit = "32be9f81c8ae1d50610ed025db0de83568b6736b"
+++
thrall is the **foot** of the four-component split (yog REMOTE §12, adopted 2026-08-28): yog REMOTE §2's tool host severed into its own separately installable component. Its entire wire surface is advertise, wait on its mailbox, post captures.

This ball is the founding itself. Deliverable: a repository that compiles, tests at the 100% floor, and states what it is.

- single binary crate `thrall`, `publish = false` until a first release is adjudicated
- zero dependencies at the skeleton; the real client's deps are a separate approval
- the house Rust standard (contained Rust) adopted from birth: the ast-grep rules that apply to a dependency-free skeleton, the clippy manifest, the 300-line cap, the 100% coverage floor
- `Makefile` is the build authority; `make check` is the whole gate
- `docs/DESIGN.md` states the role and the inherited invariants, and names yog `docs/REMOTE.md` as the protocol authority thrall implements against
- pre-commit hook: the cap and the floor, plus the no-direct-commits-to-main guard

No wire code lands here. The skeleton is a `--version`-capable binary and nothing more; every surface below is its own ball.