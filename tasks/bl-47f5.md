+++
title = "AGENTS.md lacks the machine-layer note: the xdg-scope bl-leak-gate already runs this scanner, so the wiring recipe installs a duplicate"
created = 1788060208
updated = 1788138547
claimant = "OrderFerrier"
priority = 3
root_commit = "32be9f81c8ae1d50610ed025db0de83568b6736b"
+++
The operator's machine-level bl-leak-gate plugin keys opt-in on a project shipping an executable scripts/leak-scan.sh, so thrall opted in the moment bl-e878 landed — the store gate runs at xdg scope on every relevant op on this box. scripts/thrall-leak-gate is therefore redundant here, and AGENTS.md's wiring recipe would seat a second copy of the same scan. yog's AGENTS.md carries a note recording that the machine layer runs the same scanner and what scope it needs; add the equivalent four lines to thrall's, so the recipe reads as the fallback for a box without the machine layer rather than as a step to always run.