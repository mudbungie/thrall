+++
title = "the protocol pin is 14 while the published engine speaks 15, so no foot can dial: bl-dc5f has recurred at the next release"
created = 1788745789
updated = 1788745839
claimant = "Cantaloups-T4"
priority = 1
root_commit = "32be9f81c8ae1d50610ed025db0de83568b6736b"
tags = ["usability-r2"]
+++
Round-2 install lane, clean-room container, published crates only (`cargo install thrall --locked`).

thrall 0.0.12 pins PROTOCOL 14; yog 0.0.49 speaks 15. Provisioned from the engine's own `/enroll f1 foot` envelope (four files into `<thrall-data>/wire/workspaces/home/`, plus a `tools.json`), `thrall run` answers:

    home: wire protocol mismatch: this foot speaks version 14, the engine speaks 15.
    There is no negotiation — upgrade the older component until both speak one version.
    thrall: home: wire protocol mismatch: this foot speaks version 14, the engine speaks 15. …
    EXIT=1

That is the third instance of this exact ball in this crate (bl-e0f0 at 2 vs 8, bl-f88f at 8 vs 13, bl-dc5f at 13 vs 14). Filed again because a foot that cannot dial is p1 for anyone driving the devadmin path, and the per-instance work is a re-vendor of the corpus at 15.

The class — that the pin goes stale on the engine's publish rather than on this crate's — is filed once in yog as bl-bca2 with the full ledger; this ball is the instance, not the argument.

GOOD NEWS ON THE SAME DRIVE: bl-5d62 is FIXED. The mismatch now ENDS the channel with exit 1 instead of redialling forever with backoff, and the double period is gone. Verified separately against a main-tip engine at protocol 16 with a main-tip foot at 13: one line, one exit.

ONE COSMETIC RESIDUAL, noted here rather than filed on its own: the sentence is printed twice, once as `home: <sentence>` and once as `thrall: home: <sentence>`. p4 if anybody is in the file.