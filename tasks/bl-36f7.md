+++
title = "the worktree lane's foot half: a per-tool subject_cwd consent in the operator's document, and an invocation that carries the conversation's working directory"
created = 1788151066
updated = 1788151067
claimant = "OrderArbiter"
priority = 9
root_commit = "32be9f81c8ae1d50610ed025db0de83568b6736b"
+++
yog bl-77be (evidence there): under REMOTE's total router the engine's shipped
worker grant went dead — bash/read_file/apply_patch, the only acts on a
conversation's own worktree, refuse in band, and everything an agent builds
lands in a foot's scratch directory while the workspace repo records none of
it. The ruling (REMOTE §5.4 as amended by yog bl-77be) opens a worktree lane:
a granted, unqualified worktree-subject name routes to the ONE registered
client of the workspace that advertises that name WITH consent to execute at
a caller-supplied working directory, and the invocation carries the
conversation's resolved cwd — REMOTE §5's own half-sentence, "carrying the
location on the invocation instead is the half bl-1dd3's thrall moves".

This is thrall's half:

- **Consent is the operator's, per tool, in this box's document** (`tools.json`
  entry key `"subject_cwd": true`, default false). It is part of the ADVERTISED
  element — the engine routes on it — so `tools::Tool` gains the optional
  fourth fact and the advertisement projection carries it unchanged. REMOTE
  §5.1's element grows one optional key; the wire shape moves, so PROTOCOL
  moves with it (see below).
- **The invocation gains an optional `cwd`.** Present + entry consents: the
  child runs there (overriding the entry's own `cwd`); a cwd that is not a
  live directory refuses in band — this box was named as holding that
  directory and does not. Present + no consent: an in-band refusal naming the
  config key, because a box must opt in to executing at a path a caller names
  (DESIGN §3.5's containment honesty — the directory is one of the three
  things thrall enforces, and it stays the operator's unless the operator says
  otherwise). Absent: exactly today's behavior.
- **PROTOCOL 1 -> 2 in lockstep with the engine** (yog is the protocol
  authority; its corpus ledger forced the bump for the same two shapes).

The normal consenting box is the one REMOTE §5.4 already calls the normal
install: the thrall beside the server, which actually holds the server's
worktrees. Consent on a box that does not hold them yields honest in-band
failures from the tool itself.